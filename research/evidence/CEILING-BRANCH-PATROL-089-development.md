# CEILING-BRANCH-PATROL-089 development

Date: 2026-08-12

Parent/champion: `f574d4e`.

Frozen manifest SHA256:
`1A826B829284C7AD7CB6938C7DB18AE1C2179CCDC75687EB1DD20BD94906955E`.

This independent suite moves beyond 085's seven-cell line to a nine-cell
branched roadless component with one mountain transit cell, six spots, two
active patrols and one isolated WAIT control. Six new family names and seed
ranges cover balanced and duplicate brands, a rare fork, stock races, fuel
splits and terminal forks under low/default/high fuel. The complete full-match
DP retains official-score values by canonical joint position/fuel/lifetime
state and replays its witness through the exact simulator and independent
validator.

Before score execution, the oracle's semantically redundant global `O(N^2)`
dominance loop was replaced by the identical comparisons grouped by joint
physical state. The optimized oracle reproduced the consumed 085 development
result exactly: 18 cases, oracle-vs-HEAD `4/14/0`, zero invalid and hash
`6e4072b927d9fa2a`. No heuristic prune or frontier cap was introduced.

## Development result

The one valid 18-case development sweep produced oracle-vs-champion `4/14/0`,
zero invalid, no tier-1 or tier-2 gap, four tier-3 gaps and result hash
`33080960367d1631`:

| Family | Fuel | Champion | Exact oracle | Tier-3 gain |
|---|---|---:|---:|---:|
| branched-duplicate | low | 5/20/25 | 5/20/30 | +5 |
| fuel-split | low | 6/24/25 | 6/24/30 | +5 |
| terminal-fork | low | 5/20/28 | 5/20/30 | +2 |
| branched-duplicate | high | 5/20/47 | 5/20/48 | +1 |

All other low/default/high strata tied exact, including every tier 1 and tier 2.
The largest exact frontier was 39489 states. Local elapsed is only proof-tool
tractability evidence and is not a production-performance claim.

The 54-case holdout remains sealed during read-only attribution of one new
development counterexample. No production source change is authorized until
the earliest causal loss is established independently of consumed 085/087/088
fixtures.

## Read-only attribution

On branched-duplicate/low seed `1500100`, divergence starts on day 1. The exact
day score is `5/5/11` versus champion `5/5/5`, with equal lifetime/daily tiers.
The exact plan is absent from all 16 audited F0 candidates. Route attribution
shows its three-agent mask is incomplete at the production 16-column portfolio
(`011`) and complete at 32 columns (`111`). Injecting only the missing exact
routes makes the exact master outcome available at the unchanged master cap 32;
its admissible full-match upper is joint rank 1. Thus the earliest loss is
column retention, not F0 ranking, W1 value or master capacity.

`prune_columns` already owns a spot-diversity lane, but its caller enables that
lane only when configured fuel is at least three day-step budgets. Every 089
counterexample falls below that threshold, including the registered high-fuel
profile, so serving-broad routes compete only through priority after brand and
mandatory preservation. This opens the separately registered 090 candidate:
enable the existing spot-diversity lane on deterministic roadless maps without
changing any column count, route generator, comparator or road-containing path.
