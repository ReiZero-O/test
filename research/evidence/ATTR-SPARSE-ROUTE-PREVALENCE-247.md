# ATTR-SPARSE-ROUTE-PREVALENCE-247

Status: closed positive attribution; production source unchanged.

The validated replacement completed exactly 48 result rows, 48 matching
`case_complete` rows and one `run_complete` marker. Runner stderr is empty.
The development log SHA256 is
`346CC953E2ED702C7035323D76A4FE3363C217899976418BA2AF2A67A1790425`.

Across the 24 fresh paired 8/9/10-player fixtures, forcing the fixed sparse
current-day exchange produced official lexicographic W/T/L `18/4/2`.
First-difference tiers were four ties, one tier-2 loss and nineteen tier-3
differences. Aggregate component deltas were `0/-1/+510`; serving gains summed
to `+568` with maximum `+69`, while losses summed to `-55` with minimum `-54`.

The gains are broad rather than one-fixture: players 8 were `7/2/1`, players 9
were `4/2/0`, and players 10 were `7/0/1`. Multi-team suites 12/16/24/32 were
respectively `3/1/0`, `6/0/0`, `6/0/0`, and `3/0/1`. Deadline-role fixtures
were `9/3/0`; fixed-role fixtures were `9/1/2`. Every default-fuel fixture won,
and high fuel was `5/1/0`; both losses occurred in low fuel.

The two regressions are material and prohibit directly promoting the forced
takeover arm. General seed `9620000` changed `6/24/41` to `6/23/37`, a tier-2
loss of one daily distinct. Multi-team-32 seed `9624001` changed `6/60/409` to
`6/60/355`, a tier-3 loss of 54 servings. Therefore the evidence supports only
an additive SCORE successor that keeps the parent candidate and submits sparse
challengers to the existing F0/profile/certificate selection path; it does not
support unconditional replacement.

The sparse arm examined 742 stable public `(position,fuel)` patrol
representatives, emitted and independently validated 50,488 routes, accepted
119 current-day takeovers and measured +575 immediate servings. Both arms had
zero invalid, emergency, checkpoint closed-loop, midday, terminal-sparse,
protected-terminal and public-window failures.

Functionality-preservation answers: (1) this attribution removed, disabled,
deferred or reduced no designed production functionality; (2) nothing was
deleted. A separate frozen SCORE experiment may now test additive candidate
supply. The forced attribution path itself remains research-only.
