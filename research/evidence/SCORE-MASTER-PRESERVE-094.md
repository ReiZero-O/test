# SCORE-MASTER-PRESERVE-094 rejected development

Parent `f77c101` and the candidate were built in isolated external harness trees
before their respective source states changed. The candidate added only descending
`preservedServingPotential` after ties on escort, lifetime gain, daily gain and
current serving gain.

The frozen general development seeds `3410000..3410017` covered three independent
fixtures in each of balanced, rare-brand, threshold-corridor, fuel-tight,
high-stock and overnight at the 5000-ms cap and fixed all-patrol roles. Direct
candidate-vs-parent W/T/L was `0/18/0`; every official lifetime/daily/servings
tuple tied exactly. Invalid and emergency were zero in both builds. Several
operation counts changed, confirming the order was reachable, but no score did.
Local timing was ignored.

Verdict: rejected for no causal development benefit. BTC-like development and all
holdout/target rows remained sealed. Source was restored exactly to `f77c101`.
Do not add more ordering keys or tune this tie-break on the consumed 094 seeds;
reopen only from a new independent counterexample whose earliest loss is proven
to be DFS branch order.
