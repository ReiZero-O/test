# ATTR-TANKER-TERMINAL-LOSS-139

Date: 2026-08-13  
Production parent: `5dccb0f`  
Consumed witness: 138 development rendezvous/low seed `2800000`

138 proves a structured low-fuel terminal score gap but not its cause. This
attribution exposes exact and HEAD plans, claims and traces, then runs the
ordinary production-width terminal route generator without a wall-clock cutoff
and the unchanged master. It records whether every exact per-agent action route
exists before testing synchronized team reachability.

The seed is consumed development evidence. No threshold, rank, cap or route
condition may be tuned on it, and the frozen 138 holdout remains sealed. A
production candidate may open only if the earliest missing capability has a
bounded fixture-independent construction that adds to every parent route.

Result: the production-width portfolio had widths `21|14|21` and exact route
mask `000`; its unchanged master completed at HEAD score `4/14/35`. Injecting
the exact atomic bundle made the same master complete at `4/14/37` and retain
the exact outcome. The independent portfolio completed all 1600 MCTS rollouts
without hitting its deadline but scored below HEAD. Its `HubPlan` type and
ordering support multiple windows, yet generation produces exactly one static
window per plan. The earliest loss is therefore repeated mobile rendezvous
generation, not pruning, synchronization validation, master or comparator.
