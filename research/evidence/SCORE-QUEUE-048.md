# SCORE-QUEUE-048

Parent and candidate were evaluated in the same temporary binary from restored
`afcd2da`. The only research switch selects the pending-label queue key inside
the capped anytime resource enumerator when `minimumSpots` is present. The
complete proof path, graph, dominance, reconstruction and work/route caps are
unchanged.

The registered development gate used archived `m-1285` day 10, harvest mode 7
and the 5000 ms internal cap in order shallow, cardinality, cardinality,
shallow. Both shallow runs selected official score `6/60/320`, exact seed/local
servings `34/34`, with six supported agents. Both cardinality runs selected
`6/60/323`, exact seed/local `37/37`, with six supported agents. Every run was
exact-valid and non-emergency. Thus the causal tier-3 gain is `+3` and follows
the policy rather than execution order. Local elapsed values are excluded from
the verdict.

The already-opened production-mode control `ATTR-QUEUE-046` remains neutral at
the final official score while cardinality raises exact-local servings by one.
This development evidence authorizes only the frozen screening rows at SHA256
`13EC4F536FA573AA3158D35D93CDC5A5D6F0DEBF6572E1D2BDEADDA471122231`.
It does not authorize BTC, promotion or commit.

## Frozen screen

The 27 fresh same-terminal-state pairs closed at `W/T/L = 3/24/0`. Every
first difference was tier 3. All three gains were `+1` serving and there was no
loss tail. The gains occurred in three distinct protected strata:

- low-fuel, one tanker, fuel-tight seed 972003: `424 -> 425`;
- low-fuel, two tankers, threshold-corridor seed 973004: `395 -> 396`;
- default fuel, one tanker, balanced seed 974004: `402 -> 403`.

General fixed and deadline-role lanes were `0/12/0`. The high-fuel control was
`0/3/0`; its exact settled-state counts were identical between policies, as
required for the `minimumSpots == null` proof path. Every pair used one shared
prefix, role assignment, state hash and ledger, and both repetitions of each
policy agreed on official score. Invalid and emergency counts were zero.

This positive/neutral screen opens the already frozen full rows exactly once.
BTC remains prohibited until the full paired gate closes.

During the full general deadline-role row, seeds 971010--971015 completed but a
PowerShell post-processing typo discarded the raw result lines and emitted
synthetic empty `//` fields. Those fields are invalid evaluator output and are
not score evidence. No source, binary, policy, seed set or gate changed. One
identical recovery run of exactly those six already-opened seeds is permitted
only to recapture the four raw `result` lines per seed; it may not inform any
candidate modification.

## Frozen full matrix

The full 78 unique terminal-state pairs closed at `W/T/L = 4/74/0`. Every
first difference was tier 3, every gain was `+1` serving, and the observed loss
tail was zero. Lane results were:

- general fixed plus deadline roles: `0/36/0`;
- BTC-like low fuel, one tanker: `2/10/0`;
- BTC-like low fuel, two tankers: `1/5/0`;
- BTC-like default fuel: `1/11/0`;
- BTC-like high fuel: `0/12/0`.

The two one-tanker low-fuel gains were separate fuel-tight seeds 972003 and
972009. The two-tanker gain was threshold-corridor seed 973004. The default
fuel gain was balanced seed 974004. Thus the positive tail spans independent
seeds, fuel strata, families and role compositions; it is not routed by any of
those labels. All pairs had one shared prefix/state/ledger, stable A/B repeats,
zero invalid and zero emergency. The six-seed instrumentation recovery is not
counted twice.

Together with the independent archived `m-1285` causal gain of `+3`, this is a
small but globally non-regressing official-score improvement under the frozen
local semantic gate. The temporary policy switch must now be removed and
cardinality-first installed as the single canonical capped-anytime order. Only
then may BTC target-host validity/performance be evaluated. No commit is yet
authorized.

## Canonical candidate and BTC gate

Canonicalization removed the temporary policy API, replay option and A/B harness
scaffolding. The final production logic diff against `afcd2da` is 31 lines in
`src/orienteering.cpp`; the proof path and all registered invariants remain
unchanged. The unit suite passed. A canonical replay of `m-1285` reproduced
`6/60/323`, exact-local 37, six supported agents, exact validation and zero
emergency. The canonical BTC executable SHA256 is
`CE37B6D06F5AD92CDB3921DCCA6CAE21A4D6E688431232A78A7E25DDEF2EE6CF`.

The supplied BTC administrator answer confirms the response window can vary by
round. The per-match value is therefore the outer deadline authority, while this
candidate remains internally capped at 5000 ms. A direct `POST /practice` attempt
with the bot match token returned HTTP 401 because that token cannot create
matches. The authenticated BTC UI was then used to create fresh match `m-1796`
with every advanced field explicit: hard, three bots, 10 days, 32x32, 100
steps/day, 5000 ms response time, eight agents, 12 spots, six franchises and low
fuel 1x. The runner used the frozen canonical binary above with harvest/future
mode 7/7; no rebuild or source change occurred.

`m-1796` completed 10/10 HTTP-valid applied days and reconciled 9/9 authoritative
state transitions. Independent replay-check reported exact simulator/validator
agreement on every day and reconstructed final score `6/60/312`. There were zero
deadline skips, retries, emergency fallbacks, exact deadline overruns and 5000 ms
hard-cap breaches. Server-reported response times were
`2737,2759,2731,2727,2724,2727,2729,2700,2628,1648` ms: nearest-rank p95/p99 and
maximum were all `2759 ms`. Decision `totalMs` had maximum `2699 ms`.

The candidate mechanism was exercised on authoritative day 10: six agents were
supported, the capped exact enumerator settled `7,500,000` states, and its local
route servings improved from seed `39` to `40` in `655 ms` with zero overrun.
The final certified score `6/60/312` equalled the master's optimistic upper bound;
the separate search-guidance bound `314` remained loose while search was
deadline-limited. The frozen replay SHA256 is
`DCC0A6D36DFB7C2EFECB382ED220B977E37EBA145B25EF291DAF879C079EF272`.

A fresh same-terminal development counterfactual then compiled the clean parent
`afcd2da` in an isolated worktree (parent executable SHA256
`AB2556D6436C8AB14F53D6657E2C28FBA9E504DFD4BDF477A80C6CEC960D618A`) and solved
only authoritative `m-1796` day 10 with the recorded prefix ledger and state.
Candidate and parent were run in reverse order twice. Candidate was stable at
`6/60/312`, exact seed/local `39/40`; parent was stable at `6/60/307`, exact
seed/local `35/35`. Both sides supported six agents and had zero emergency,
exact overrun or validation failure. This is a new tier-3 development gain of
`+5` on the BTC terminal state. It is not added to the frozen holdout and no
local elapsed value is used. Because the recorded prior trajectory belongs to
the candidate, only the identical terminal state comparison is attributed; this
does not claim a paired whole-match result.

The team ranked first over the three practice bots, but this is recorded only as
the asymmetric BTC failure gate, never as quality or promotion evidence. The
target-host validity/lifecycle/hard-cap gate passes for this match. The diagnostic
manifest still reports `competitionReady=false` because `p99Calibrated=false`;
one ten-day match cannot silently rewrite that calibration. SCORE-QUEUE-048 stays
active and uncommitted pending diverse real-opponent evidence; a non-paired match
may expose a counterexample but cannot by itself prove superiority to the parent.

## Archived real-opponent controls

The only two complete archived team-vs-team replays were inspected as independent
observational controls, not as a tunable holdout. `m-1038` and `m-1042` each have
two teams, four days, an 8x8 map, 32 steps/day, 60-second outer windows, fuel 64,
four agents, eight spots and four brands. Their live standings were respectively
`4/16/60` versus `4/16/52` and `4/16/59` versus `4/16/53`.

On the identical recorded terminal state and ledger, candidate and clean parent
both returned byte-identical plans and official scores: `4/16/60` on `m-1038`
and `4/16/59` on `m-1042`. Selected scores equalled both optimistic and guidance
upper bounds; exact validation passed with zero emergency or overrun. Replay
SHA256 values are
`B5B9390D9946620652B7F63358CFC22FD3BDDB378311FA266A3314146AAB12D7` and
`913DE968028031F4D48B1607478D167342C6054AD4CC4B8463B0A6FD1E3619AC`.
This is a `0/2/0` terminal control showing no regression on archived real-opponent
short/small-map behavior. It is not promotion evidence for the changed capped
queue because the two fixtures share one narrow configuration and do not expose
the diverse long-horizon low-fuel lane where the candidate gains. A fresh real
opponent on competition configuration remains required.

## Next-gap attribution on m-1796

The fresh target replay was also inspected to prevent a circular successor from
being opened on a loose bound. On days 1--8, incumbent-versus-optimistic serving
tails were `+1,+3,+5,+2,+1,+2,+3,+3`; however every exact-evaluated audit
candidate had the same official current-day score as the selected incumbent.
There is no feasible better witness in the recorded pool. Day 9 optimistic and
guidance bounds equal the selected current-day score. On day 10 the optimistic
bound equals selected `6/60/312`; guidance `6/60/314` is strictly looser and is
not a feasible counterfactual.

Therefore `m-1796` opens no post-QUEUE master/evaluator score mechanism. The
master hit its deadline, but deadline status plus an unwitnessed upper bound is
not evidence that more master search would improve official score. Reopen that
axis only when an exact-valid higher-scoring bundle or a first-differing feasible
candidate is produced. This conclusion does not claim architectural convergence;
it only prevents optimizing the next stage from telemetry that lacks a witness.
