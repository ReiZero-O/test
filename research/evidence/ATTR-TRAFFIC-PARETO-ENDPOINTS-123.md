# ATTR-TRAFFIC-PARETO-ENDPOINTS-123

Parent: `5dccb0f`

The sidecar retained the deduplicated union of the two independently exposed
per-terminal endpoints: minimum-traffic fuel-first and minimum-traffic official-
contribution-first. On consumed seed1700000 day 1 it naturally contained all
three oracle routes (`route_mask=111`) with widths `10|12|1`.

The unchanged master cap32/diversity8 visited 240 nodes and returned 32
candidates, but retained neither the exact oracle plan nor an equivalent joint
outcome (`exact=0`, `outcome=0`). Route representation was repaired, yet
post-hoc master retention still erased the globally useful combination.

Verdict: rejected. Per the preregistered final gate, the terminal-sidecar axis is
closed; endpoint ranks, candidate cap and diversity may not be tuned on this
consumed witness. All capability source is restored to canonical `5dccb0f`.
Only an independently justified master-aware pricing/value mechanism can reopen
the broader traffic representation gap.
