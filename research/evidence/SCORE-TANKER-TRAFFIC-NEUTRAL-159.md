# SCORE-TANKER-TRAFFIC-NEUTRAL-159

Date: 2026-08-14
Production parent: `cf7e4b4`
Frozen tanker manifest: `research/holdouts/SCORE-TANKER-TRAFFIC-NEUTRAL-159.csv`
SHA256: `8B9F8EB2232ECD89EF7A8AEE16E28FA28B274D903327F16548F719D65080F2FA`
Frozen protected matrix: `research/holdouts/SCORE-TANKER-TRAFFIC-NEUTRAL-159-MATRIX.csv`
SHA256: `5EF08D907E76DD17E3A234D05B8EF49BD2922A7DF6065B27C1D01B1866F70F82`

## Registered hypothesis

158 proved the canonical caravan can unlock exact stock-ceiling serving gains,
but changed road traffic creates larger future losses on the general matrix.
159 retains the same provider construction and timestamped refuel coverage but
admits a newly synthesized multi-waypoint provider only when its exact
`fullFootprint` is empty. The certificate is attached to each route and uses no
map, seed, family, fuel, role, bot or match identity.

All parent route, rendezvous, escort and tanker columns remain active after the
unchanged parent pruning and are never re-pruned. No designed functionality is
removed, disabled, deferred or reduced, and nothing is deleted. The official
lexicographic comparator, exact simulator, independent validator, master,
role-selection path and final certification remain unchanged. The provider
endpoint/order/cap are inherited unchanged from 158. Internal compute remains
hard-capped at 5000 ms; local elapsed time is never promotion evidence and BTC
target-host telemetry is final for performance.

## Gate order

1. Consumed activation anchor `3200100` must retain exact `3/12/16`.
2. Fresh tanker development (12) and fresh general fixed development (12).
3. Open each sealed tanker/general/roadless lane once only if development passes.
4. Run BTC validity, lifecycle and target-host hard-cap telemetry only after all
   semantic gates pass.

## Development result

The consumed anchor retained exact `3/12/16` and day-1 `3/3/4`. Fresh tanker
development was `2/10/0`, first-tier gain/loss `4/0`, invalid `0`. Fresh
road-containing fixed development was `3/9/0`, gain/loss `7/0`: one tier-2
gain `+2` and two tier-3 gains `+1/+4`, with zero invalid, emergency and
deadline days. Both gates passed before any holdout was opened.

## Holdout result and verdict

Tanker holdout was `6/30/0`, gain/loss `11/0`, invalid `0`. The first general
fixed holdout was `6/25/5`, all tier 3, gain/loss `18/11`; balanced was
systematically `0/3/3`, loss `9`. On consumed seed `3760032`, both searches
completed: candidate changed a day-1 official-score tie, then lost four servings
on day 3 and two on day 4 (`5/20/41` versus `5/20/47`). This is a real future
state regression, not a local cutoff artifact. Remaining protected lanes and
BTC were not opened after the blocker.

Status: rejected. Reopen only through structural parent-tie protection before
secondary terminal/traffic tie breaks; never route by family.
