# SCORE-TANKER-CANONICAL-CARAVAN-158

Date: 2026-08-14
Production parent: `cf7e4b4`
Frozen manifest: `research/holdouts/SCORE-TANKER-CANONICAL-CARAVAN-158.csv`
SHA256: `F9FC8A1962CCE1B6C7F5408273087F7C91418B08F9D7F4BC2A50B78982E98473`

157 proved timestamped required/provided refuel coverage is accepted by the
unchanged master, but its independent tanker providers contain only one route
leg followed by waiting. 158 materializes the already-attributed bounded
multi-waypoint provider construction inside the canonical route portfolio:
stable permutations of at most four spots ordered only by public official
priority, dual-exact tanker columns, then patrol join/follow/detach columns
covered by the same provider's exact timeline.

This is additive after unchanged parent pruning; parent columns remain and no
second pruning pass is permitted. There is no atomic whole-plan comparator and
no seed/map/family/fuel/role/bot dispatcher. The endpoint limit is unchanged
from the previously registered construction rather than tuned on 157. Existing
`maximumEscorts` bounds providers. Official lexicographic score, exact simulator,
independent validator and master certification are unchanged. Local elapsed is
excluded; BTC target-host remains final for performance.

Unit suite passed. The consumed anchor activated exactly: final score
`3/12/15 -> 3/12/16` and day 1 `3/3/3 -> 3/3/4`; the candidate came through
normal certified route/master selection. Fresh paired development was `3/9/0`,
invalid `0`. Rendezvous-chain/low gained `+1`, stock-cycle/low `+2`, and
rare-return/low `+4`; candidate scores `3/12/16`, `2/8/20`, and `2/8/24`
respectively equal the public whole-match sum-of-stock ceilings. All other
fuel/family rows tied. Holdout remains sealed pending one paired execution.

The holdout was opened once: candidate versus parent `8/28/0`, invalid `0`.
All differences were tier-3 gains of `+1..+4`; total gain was `15`, loss `0`.
Wins covered all four structural families at low fuel, while default/high tied.
Stock-cycle winners reached `20` and the strongest rare-return winner `24`,
matching their public stock ceilings.

Roadless protected lanes remained broadly positive: fixed `98/106/12` with
first-tier gain/loss `319/18`, exhaustive `27/41/4` with `96/5`, and deadline
role `32/38/2` with `90/2`. Invalid and emergency counts were zero and every
family was net positive. The previously consumed exhaustive-role regression
fixture was repaired (`3/8/8`, mask 2, to `3/10/17`, mask 4).

The full road-containing fixed lane rejected 158 at `23/68/29`, gain/loss
`61/70`. Four of six families were net negative and one candidate deadline day
occurred. On consumed seed `300030`, candidate tied day 1, gained one serving
on day 2, then its changed road footprint lost one lifetime-daily contribution
on day 4 and ended `6/23/36` against parent `6/24/38`; both searches completed.
This attributes the blocker to future own-traffic externality. Native/deadline
road-containing lanes and BTC were not opened after the blocker. Verdict:
rejected; do not rescue with a roadless/map dispatcher.
