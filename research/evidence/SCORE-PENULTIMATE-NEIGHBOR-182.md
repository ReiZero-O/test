# SCORE-PENULTIMATE-NEIGHBOR-182

Date: 2026-08-21  
Parent: `828ea78`  
Status: rejected on first consumed causal gate  

After the complete parent result, enumerate the existing exact resource frontier
for each fuel-constrained patrol within an equal share of real pre-network slack.
Each completed exact route replaces only that patrol's actions in a copy of the
parent plan. Other agents remain byte-identical. Exact simulation and the
independent validator reject stock, traffic or synchronization conflicts.

The neighborhood is additive and linear in the exposed per-agent routes; it does
not form multi-agent Cartesian products. Candidate future profiles use the same
public manifest and may replace parent only by complete penultimate bound closure.
Incomplete enumeration, certification or deadline returns parent.

Frozen manifest: `research/holdouts/SCORE-PENULTIMATE-NEIGHBOR-182.csv`  
SHA256: `A366F7E16D794696D36DFB457B0B775C9BC1DDC4D56E1CE0DDE217D441248516`

## Result

Compile and unit gates passed. On consumed `1720000`, complete exact enumeration
from the unchanged parent penultimate state produced exactly one non-parent
single-agent replacement. It remained current `6/9/9` and certified exactly to
`6/11/11`; no takeover occurred and final score tied parent `6/11/11`.

The known mask-`111` route and `6/12/12 .. 6/13/13` profile are reached only
after applying the oracle day-1 prefix. They are not missing from the parent
day-3 frontier. The gap is therefore multi-day path-dependent. Other anchors,
consumed 176 fixtures, fresh development and holdout remained unopened. This
candidate is rejected and production source is reverted.

Reopen only with a complete multi-day closed-loop evaluator inside `5000 ms`
that also proves generality beyond the artificial one-active low-fuel fixtures.
