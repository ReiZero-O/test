# SCORE-MASTER-PORTFOLIO-096

Parent: `f77c101`

Mechanism: increase only the Normal-deadline master population from 32 to 48,
preserving the one-quarter diversity ratio. Maximum combinations remained 40000;
the F0 intake remained 16 candidates and 24 operations. Route generation,
harvest extensions, bounds, bundles, exact simulator, independent validator,
roles, deadlines and the 5000-ms hard cap were unchanged.

The value 48 was preregistered from the fresh 095 attribution before candidate
score: seed 1600000 first retained the exact day-1 outcome at master cap 48.
No alternative population size was tested on the consumed suite.

Development candidate-vs-parent was `2/15/1`, zero invalid. Two tier-2 gains
improved cycle-duplicate low from `5/17/29` to `5/19/29` and fuel-circuit low
from `6/22/25` to `6/24/28`. The only loss was terminal-loop low at tier 3,
`5/20/30` to `5/20/29`.

The immutable 54-fixture 095 holdout was then opened exactly once. The complete
oracle saw candidate gaps `12/42/0`, zero invalid, result hash
`970c5fc6749f87ed`. Direct HEAD-only candidate-vs-parent comparison used binaries
from the same research probe. Candidate SHA256 was
`84CB2B39DD055E0FE045C31227B3C2AF25E925785D17E05B4D63A0CE34ADF3D2`;
parent SHA256 was
`0E253EEFE69B56404A19D77FFC49B8A08CB1F1AAC2E44CF9D821E065C89300B8`.
Both P/C orders produced the identical paired result: `8/43/3`, zero invalid,
tier-2 gain/loss sums `14/1`, and tier-3 gain/loss sums `0/5`. The largest loss
was four servings; no tier-1 loss occurred. This was a broad positive roadless
trade-off, not absolute dominance.

The unchanged candidate then faced the previously consumed road-containing
protection seeds `830000..830035` in fixed all-patrol mode at 5000 ms. The first
parent/candidate order returned `2/27/7`, zero invalid/emergency, with tier-2
gain/loss `0/6` and tier-3 gain/loss `4/7`. All nine changed seeds were rerun in
reverse order. Seven losses and both gains persisted. Rare-brand seed 830011
shifted from a tier-2 loss to a tier-3 loss, but the material blockers remained:

- overnight seed 830021: parent `5/25/53`, candidate `5/24/47` (tier 2, -1);
- rare-brand seed 830029: parent `5/20/31`, candidate `5/16/28` (tier 2, -4);
- five additional stable tier-3 losses across threshold-corridor, balanced,
  rare-brand and terminal behavior.

This is a systematic road/traffic downside and violates the protected matrix.
Native-role and BTC lanes remained unopened because they could not rescue a
candidate already blocked by causal score regression. The one production line
was restored to parent. Reopen only from a fresh counterexample that derives a
semantically safe population-retention rule distinguishing future-useful states
without tuning 32/48, fuel or road guards on consumed 095/096 fixtures.
