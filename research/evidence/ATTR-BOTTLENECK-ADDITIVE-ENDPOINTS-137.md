# ATTR-BOTTLENECK-ADDITIVE-ENDPOINTS-137

Date: 2026-08-13  
Parent: `5dccb0f`  
Consumed fixture: 135 fuel-bottleneck/low seed 2700400, day 1

136's endpoint-only projection omitted agent 0's exact state class, but that
route is already retained by the ordinary width16 portfolio. This probe keeps
the full non-exact parent width16 route set and every atomic bundle, then adds
the frozen 136 endpoint union without eviction or replacement. It runs the
unchanged master once. Passing requires exact plan or equivalent outcome.

This is research-only capability attribution. It does not authorize wide
production generation; a pass still requires proof that the added endpoints
are naturally constructed before ordinary pruning and a parent-protected
integration under the 5000-ms hard cap.

Result: rejected. The additive union preserved all parent routes, reached exact
route mask `111` at widths `25|24|2`, but the unchanged master retained neither
exact plan nor equivalent outcome among 32 candidates after 498 nodes. This
closes the endpoint axis; no third endpoint, width/cap increase or supplemental
solver is authorized.
