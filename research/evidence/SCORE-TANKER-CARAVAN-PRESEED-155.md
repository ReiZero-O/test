# SCORE-TANKER-CARAVAN-PRESEED-155

Date: 2026-08-14
Production parent: `cf7e4b4`
Frozen manifest: `research/holdouts/SCORE-TANKER-CARAVAN-PRESEED-155.csv`
SHA256: `81F341E7336CF6144C4EC795352D148868BCC75C2F98EBAD9C4140910FDE1DC9`

Accepted attribution 154 proves a join-and-follow convoy can exactly realize the
missing day-1 serving. Candidate 155 pre-generates a fixed-operation atomic
convoy set before the independent static search: all stable permutations of at
most four highest official-priority spot endpoints, capped at 24 routes. Patrols
join at their earliest fuel-feasible action boundary and copy the remaining
tanker actions. Every plan is dual-exact evaluated.

The outer production incumbent and main route/master candidates remain intact;
the independent lane is additive. A convoy may replace the independent
incumbent only on a strict official score gain and only behind the roadless,
all-brand/all-stock, full-patrol-fuel and common-terminal guard. Terminal-day
behavior is unchanged.

Anchor `3200100` must close before any fresh row opens. No timing result from
local execution has performance authority.

## Development result

Unit suite passed. On consumed anchor `3200100`, day 1 improved from parent
`3/3/3` to exact `3/3/4`; all agents ended full and co-located at spot 34.
However final score regressed from parent `3/12/15` to `3/12/14`. On day 2 the
candidate left spot 34 immediately and collected only three servings, omitting
the explicit `WAIT(1)` required to harvest a start-of-day spot.

Verdict: rejected and source restored. No fresh candidate score was run. The
only admissible successor is canonical overnight-harvest composition; no convoy
width/order/join or guard tuning.

## Holdout result

Sealed and unopened.
