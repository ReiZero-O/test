# FINAL-QUEUE-065 BTC closure

Date: 2026-08-10

Frozen executable SHA256:
`8A8AD48AD5A9227AA43C9FF685D20CE7CA1EF657EBCDFC2A675C6FEA9A16816D`.

Twelve preregistered no-replacement practice matches were created through the
authenticated BTC UI with every advanced field explicit: hard, three bots, ten
days, 32x32, 100 steps/day, 5000 ms response, eight agents, twelve spots and six
brands. Four matches each used low/default/high fuel. Setup replay audit matched
the manifest in all twelve cases.

The aggregate target-host result is 120/120 HTTP-valid accepted actions and
108/108 independently replay-reconciled authoritative transitions. There were
zero emergency decisions, deadline skips, server WAITs, fallback actions,
transport retries, WinHTTP 12002 failures and 5000 ms hard-cap breaches. Maximum
decision `totalMs` was 2753 ms. Maximum exact-orienteering deadline overrun was
6 ms in one high-fuel decision; it was bounded by cancellation and produced no
submission or lifecycle failure. All other matches recorded zero exact overrun.

Every match happened to rank first, but rank is ignored and supplies no quality
authority. Quality authority remains the frozen paired `26/196/1` score matrix.
This BTC batch proves configuration, protocol, validator, lifecycle and target
runtime for the clean final binary. Per-match scores, hashes and tails are in
`FINAL-QUEUE-065-btc.csv`.

The same frozen binary entered the real-opponent queue once after the BTC batch.
The queue expired without another team and created no match. Archived human
terminal controls remain neutral at `0/2/0`. Promotion is accepted from the
frozen paired score evidence plus the completed target-host gate; the missing
fresh non-paired opponent is recorded as an external evidence limit and prevents
any claim of full architectural ceiling.
