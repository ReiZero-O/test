# SCORE-POST-PARENT-EXACT-180

Date: 2026-08-21  
Parent: `828ea78`  
Status: rejected on consumed causal anchors  

## Gap and causal boundary

Exact experiments 175/177 prove three independent pre-terminal fuel-value gaps.
The broad in-pipeline exposure in 176 closed the first anchor but lost on six of
twelve fresh general fixtures. Experiment 179 showed that adding the omitted
legacy checkpoint did not change any of those outcomes. The remaining regression
mechanism is displacement inside the shared bounded master/ALNS/F0 pipeline.

Experiment 180 moves the additive boundary after the complete parent result.
Parent selection, certification and independent validation finish unchanged.
Only then may the existing deadline-aware fuel-exact route kernel consume real
slack before the network reserve. The exact challenger cannot modify parent
generation, ranking, audit or plan. It replaces parent only after independent
validation and complete bound-closed takeover against the protected parent.

## Proof obligations

- No sidecar start or an incomplete sidecar returns the exact parent.
- A generated challenger without complete scenario certification returns parent.
- Takeover requires challenger survival lower bound to meet parent valid-upper
  survival at every official lexicographic threshold, with strict challenger
  upper upside at some threshold under identical scenario weights.
- The selected challenger passes exact simulation and the independent validator.
- The solver stays inside `5000 ms`, the `1600 ms` BTC network reserve remains
  untouched, and at least the existing `25 ms` final-validation floor remains.
- The sidecar is enabled only by public horizon position and fuel constraint; no
  seed, family, map, bot or replay discriminator is permitted.

Target-host archive evidence contains 110 decisions: 57 had less than 25 ms of
pre-network slack, while 35 had at least 500 ms. Therefore the mechanism must be
deadline-anytime and is expected to be inactive on many days. Timing measured
locally has no promotion authority.

Frozen manifest: `research/holdouts/SCORE-POST-PARENT-EXACT-180.csv`  
SHA256: `B9817875EF78952A8A2E63EEFEA46EFAD68F04477CB0EECC3ACA3B18BAEEB463`

## Result

The existing exact kernel completed and certified challengers on all three
consumed anchors. Results against unchanged HEAD were `0/2/1` W/T/L:

- `1720000`: day-1 bound-closed takeover, final tie `6/11/11`;
- `1721000`: day-1 bound-closed takeover, regression from `5/10/11` to
  `5/9/10`;
- `1722000`: no takeover, tie `5/11/11`.

Thus post-parent placement removes shared-pipeline displacement, but the static
scenario certificate is not a closed-loop no-regression proof across multiple
future replans. The first-day mechanism is rejected before consumed 176 rows,
fresh development or holdout. It may reopen only with a complete closed-loop
evaluator inside the hard cap, not a family/seed guard.
